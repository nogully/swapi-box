/* eslint-disable */
import React from 'react';
import ReactDOM from 'react-dom';
import { apiGet } from './api';
import { shallow } from 'enzyme';

describe('api', () => {

//   it('exists and matches snapshot', () => {
//     expect(wrapper).toBeDefined();
//     expect(wrapper).toMatchSnapshot();
//   });


// //fetch is called with correct params
//   it('calls fetch with the correct params', () => {
//     const mockData
//     const expectedParams
//     window.fetch = jest.fn().mockImplementation(()=>{
//       return Promise.resolve({
//         ok: true,
//         status: 200,
//         json: () => Promise.resolve({
//           groceries: mockGroceries
//         })
//       })
//     })
//     addGrocery(mockGrocery)
//     expect(window.fetch).toHaveBeenCalledWith(expectedParams)
//   })

//   it("returns an object if it comes back OK", () => {

//   })

// //gets an error if status code is not OK
// window.fetch = jest.fn().mockImplementation(()=> Promise.resolve({ status: 500})
//     })

})